# discord-claude-integration

DiscordとClaude Codeをシンプルに連携します。

# Installation

`bun i`して.env.exampleを参考に.envを書いて`bun index`してください。

# Usage

Discordで`/cc`してスレッドを作成します。

既存のClaude Codeスレッドと、Discordのスレッドをリンクしたい場合は、Discord側のスレッド一件目に、`cc:Claude側のスレッドID`を宣言してください。
